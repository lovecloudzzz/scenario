import React from 'react';
import styles from "./FilterAcceptButton.module.sass"
export const FilterAcceptButton = () => {
    return (
        <button className={styles.AcceptButton}>Подтвердить</button>
    );
};